package pages 

import (
	"github.com/zmzlois/go-tooo/internal/templates/shared"
	"github.com/zmzlois/go-tooo/internal/templates/partials"
)

var data map[string]string = map[string]string{
	"avatar": "https://pbs.twimg.com/profile_images/1751005853900005376/nGX46ucY_400x400.png",
	"name":   "LOIS",
	"bio":    "Noob dev",
}

// var data map[string]string = map[string]string{
// 	"avatar": "",
// 	"name":   "",
// 	"bio":    "",
// }

// func init() {
// 	err := godotenv.Load()
// 	if err != nil {
// 		panic(err)
// 	}
// 	data["avatar"] = os.Getenv("AVATAR")
// 	data["name"] = os.Getenv("NAME")
// 	data["bio"] = os.Getenv("BIO")

// }
templ HomePage() {
	@shared.Page("Home") {
		@shared.Background() {
			@shared.Container() {
				@partials.Header(data["avatar"], data["name"], data["bio"])
				@partials.LinkSection(false)
			}
			@shared.Container() {
				@partials.Footer()
			}
		}
	}
}
