package auth

// get the users bearer access token.
// func (c *Client) GetOnlyAccessToken(code string) (string, error) {

// define variables

// var (

// 	// The access token request body
// 	tokenBody []byte = c.AccessTokenBody(code)
// 	//Access token request object
// 	token = c.GetAccessToken(tokenBody, false)
// )

// fmt.Println("[GetOnlyAccessToken]: ", token)

// The Bearer access token
// var accessToken string = token["token_type"].(string) + " " + data["access_token"].(string)

// Return the bearer token from said data
// return accessToken, nil

// 	return "", nil
// }
